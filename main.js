// indecAdd is itself a function

const { add, power } = require("./add");

const indexSub = require("./subtract");

const indexMul = require("./multiply");

const indexDiv = require("./divide");

add(15, 5);
indexSub(15, 5);
indexMul(15, 5);
indexDiv(15, 5);

power(2, 3);
